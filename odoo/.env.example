# Copia este archivo a .env ("cp .env.example .env") y cambia la contraseña
# antes de levantar los contenedores. El archivo .env real NO se sube a git.

DB_USER=odoo
DB_PASSWORD=cambia-esta-contrasena
ODOO_VERSION=19.0
ODOO_PORT=8069
